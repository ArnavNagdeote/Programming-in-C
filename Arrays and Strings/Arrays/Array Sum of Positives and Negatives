#include <stdio.h>
int main() {
	int n, i, arr[100];
	int sumPos = 0, sumNeg = 0;
	
	scanf("%d", &n);

	for (i =0; i<n; i++) {
		scanf("%d", &arr[i]);
		if(arr[i] > 0)
			sumPos += arr[i];
		else if(arr[i]<0)
			sumNeg += arr[i];
	}
	printf("%d\n", sumPos);
	printf("%d\n", sumNeg);
	return 0;
	
}
