#include <stdio.h>

int main() {
   int n, i, j;
	int matrix[20][20];
	scanf("%d", &n);
	for(i = 0; i<n; i++) {
		for(j=0;j<n;j++) {
			scanf("%d", &matrix[i][j]);
		}
	}

	for(j=0;j<n;j++){
	for(i=0;i<n;i++) {
		printf("%d", matrix[i][j]);
		if(i<n-1)
		printf(" ");
		
	}
		printf(" \n");
		
	}

    
    
    
	return 0;
}
